services:
  web-bimbel:
    image: web-bimbel:latest
    build: .
    container_name: web-bimbel
    restart: always
    ports:
      - "2001:80"
    networks:
      - web-bimbel-net

networks:
  web-bimbel-net:
    driver: bridge
